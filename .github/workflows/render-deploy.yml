name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Trigger Render Deployment
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Deploy Hook
        run: curl -X POST "https://api.render.com/deploy/srv-d9c9g8b7uimc73d2bj5g?key=DPvmepQX4jQ"
